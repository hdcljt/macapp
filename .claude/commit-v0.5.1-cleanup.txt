【需求/缺陷描述】: v0.5.1 - universal 简化 + 在线更新 polish
【需求/缺陷单号】: 无
【修改内容】:
- CI 简化：删除 build-macos 分架构 matrix job，只保留 universal（一次产 arm64+x64+universal 三套 dmg/zip）
- CI 简化：删除 release job 的多架构 cleanup 步骤（无跨 job 冲突后无需清理）
- CI 简化：删除 ! -name 'latest*.yml' 兜底规则
- 文档：README「5 秒内」改为「启动后」（实际是 electron-updater 内部 backoff，非我们代码控制）
- 文档：README macOS 签名说明加 Gatekeeper 首次启动提示
- updater.ts：quitAndInstall(silent=true) 注释明确「仅 Windows 生效」
- updater.ts：progress/downloaded/error 事件在窗口已关时跳过 IPC 推送（不再噪音日志）
- updater.html：CSP 收紧（style-src 去掉 unsafe-inline），display:none 改 hidden 属性
- updater.js：isMac / notesText 局部内联；notesText 渲染注释说明 XSS 安全性（textContent）
- 规范：spec height 320→360，parent 选择器加模块作用域说明
- 版本号升级 0.5.0 → 0.5.1
